# Copy this file to .env and fill in your values.
# docker compose automatically loads .env

# ── Docker Hub image ──────────────────────────────────────────────────────────
DOCKER_IMAGE=yourdockerhubname/wiki-qa:latest

# ── Security ──────────────────────────────────────────────────────────────────
# JWT signing secret. Required in production — generate a fresh one with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Left blank, the app generates a random per-process secret and tokens are
# invalidated on every restart.
JWT_SECRET=

# Optional API key for X-API-Key header. Leave blank to disable.
API_KEY=

# ── LLM Provider ─────────────────────────────────────────────────────────────
# Options: groq (FREE, recommended), local (flan-t5-small, no API key), openai, anthropic
LLM_PROVIDER=groq

# Groq — FREE, fast, llama-3.3-70b. Get key at https://console.groq.com
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Required if LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Required if LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001

# ── Vector Store ─────────────────────────────────────────────────────────────
# Options: faiss (local, default), pinecone (serverless)
VECTOR_STORE=faiss

# Required if VECTOR_STORE=pinecone
PINECONE_API_KEY=
PINECONE_INDEX=wiki-qa

# ── Feature flags (set to "false" to save RAM on t2.micro) ───────────────────
ENABLE_RERANKER=true
ENABLE_GENERATOR=true

# ── EC2 deployment ────────────────────────────────────────────────────────────
# BACKEND_URL=http://<EC2_PUBLIC_IP>:8000

# ── Observability ─────────────────────────────────────────────────────────────
# Grafana admin password. Required — docker compose refuses to start without it.
GRAFANA_PASSWORD=
